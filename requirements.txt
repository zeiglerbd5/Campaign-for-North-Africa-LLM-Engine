# Core (required for any run)
openpyxl>=3.1
requests>=2.31

# RAG pipeline (situation engine queries rules/doctrine/playbook)
chromadb>=0.5
sentence-transformers>=2.7

# MLX backend (Apple Silicon only; install separately if needed)
# pip install mlx-lm

# Anthropic backend (cloud inference via Claude; optional)
# pip install anthropic

# Bedrock backend (AWS-hosted Claude via boto3; optional)
# pip install boto3
